<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.sky.mapper.SetmealMapper">

    <insert id="inset" useGeneratedKeys="true" keyProperty="id">
        INSERT INTO setmeal(category_id, name, price, description,
                            image, create_time, update_time, create_user, update_user)
            VALUE (#{setmeal.categoryId}, #{setmeal.name},
                   #{setmeal.price}, #{setmeal.description},
                   #{setmeal.image}, #{setmeal.createTime},
                   #{setmeal.updateTime}, #{setmeal.createUser},
                   #{setmeal.updateUser})
    </insert>
</mapper>